# Partner RAG API (primary data source)
# Point to wherever the rag-api backend is running
PARTNER_API_URL=http://localhost:8000

# Anthropic API Key (fallback if RAG API is unavailable)
# Get your key from https://console.anthropic.com/
ANTHROPIC_API_KEY=
